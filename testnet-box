tnb-cmds = (init clean)
btc-cmds = (start stop getnewaddress getinfo listtransactions sendtoaddress etc)


init (n >= 2) :
	for n:
		mkdir $n/testnet3
		cp data-template/testnet3/* $n/testnet3
		cp data-template/bitcoin.conf $n/bitcoin.conf
		#do some magic on $n/bitcoin.conf
			#replace ports


clean (n=id or n=null/ALL):
	for n:
		rm -rf $n/

	init